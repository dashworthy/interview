---
description: Resume a prior interview, targeting its open threads without re-litigating settled decisions, and update the same handoff document.
argument-hint: "[path to a prior interview-*.md]"
---

Resume a structured interview from a prior handoff document: **$ARGUMENTS**

If no path is given, use the most recently modified `.interviews/*/interview.md` (say which session you picked).

Follow the `conducting-interviews` skill, section **"Resuming an interview (open-thread mode)"**. In short:

- Read the prior handoff; parse its **Open threads** (your targets) and **Decisions** (locked — don't re-ask them).
- Confirm which open threads to tackle now, as a structured, recommendation-first question with an exit option.
- Interview only those threads, applying the four laws; keep hunting blindspots within them.
- When done, **update the same file**: move resolved threads into Decisions, append a "Resumed &lt;date&gt;" transcript block, and rewrite Open threads to what remains. Do not create a new handoff.
