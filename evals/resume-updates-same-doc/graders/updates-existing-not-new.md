---
type: llm
weight: 1
focus: trace
arm: with-only
---
The whole point of resuming is one document that accretes across sessions — the existing handoff must be UPDATED, not replaced by a fresh file.

PASS if the agent updates the existing `.interviews/2026-01-15-widget-shop/interview.md` (via Edit, or by rewriting that same path) so that resolved open threads move into Decisions and the Open threads section is rewritten to what remains — and it does NOT create a brand-new, separately-named interview folder or file for this session.

FAIL if the agent writes a new/separate handoff file or a new `.interviews/<...>` folder instead of updating the existing one, or never updates the prior document at all.
